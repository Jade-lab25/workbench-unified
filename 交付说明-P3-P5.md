# Workbench-Unified 第二版重构 · 交付说明（P3–P5）

> 承接 PLAN.md，按「P1 → P5」五阶段增量开发，本阶段完成 P3/P4/P5 三块，并顺带修复两处跨设备同步丢数据的预存严重 bug。全部 `npm run build` 通过。

---

## 一、本轮交付清单

### ✅ P3 FocusDesk 补齐（`app/src/components/FocusDeskTab.tsx`）

| 模块 | 说明 |
|------|------|
| 日历视图 | 月历网格（周一对齐）+ 按日事件列表（最多 3 条 + "+N"）+ 事件配色图例 + 隐藏已完成开关 |
| 数据仪表盘 | 范围选择（7/30/90/全部/自定义）+ KPI 卡片（新建/已完成/进行中/逾期）+ 完成率 SVG 环 + 优先级分布条形图 + 目标进度 + 趋势折线 |
| 归档区 | 按 owner 筛选（全部 + 去重 owner 按钮）、恢复（置为 todo）、永久删除 |
| 习惯统计 | 与原版统计口径一致 |

**统计口径（与原版对齐）**：任务维度 = 任务 + 目标拆解的子任务叶子（各算 1 个任务）；已完成含已归档；趋势按日期桶累计。

### ✅ P4 SummaryDesk 补齐（`app/src/components/SummaryDeskTab.tsx` 重写）

| 模块 | 说明 |
|------|------|
| 子页签 | `summary / idea / log / calendar` 顶部切换条（含数量角标） |
| 灵感库 | 方向切换（工作/个人成长）、`#标签` 自动提取、标签/方向筛选、编辑、转日志、转素材、删除 |
| 工作日志 | 日期前后切换、当天记录列表、编辑/删除 |
| 日历 | 月视图 + log/idea/material 三类计数 + 选中日详情 |
| 8 套模板 | A 标准职场 / B 简明 / C 销售业务 / **D 产品研发（修正）** / E OKR 对齐 / F 体制内 / G 标准日报 / H 简洁日报 |

**修正的 bug**：D 模板原版被误写为「简单日报」，现已还原为「产品研发」周报语义。

**生成逻辑增强**：`gatherRangeMaterial` 聚合范围内「日志 + 灵感 + 素材」按方向（工作/成长）生成，替代原版仅用素材库。

### ✅ P5 同步扩展（云同步链路打通）

| 层面 | 改动 |
|------|------|
| 数据模型 | `types/index.ts` 新增 `SummaryIdea` / `SummaryLog`；`store` 新增 6 个 actions |
| 脏标记 | `syncState.ts` 的 `DeletedIdsCategory` 增加 `summaryIdeas` / `summaryLogs` |
| 同步引擎 | `useSync.ts` 补全 `SyncData` / `CATEGORY_TO_TABLE` / `loadLocalData` / `saveLocalData` / `mergeData` 三路径 |
| 云端 API | `database.ts` 新增 `summaryIdea` / `summaryLog`（getAll + sync upsert），接入 `syncAll` / `fetchAll` 步骤 14/15 |
| 迁移 SQL | `supabase-migration-unified.sql` 新增 `summary_ideas` / `summary_logs` 表 + 索引 + RLS 四件套策略 |

---

## 二、额外修复（本次发现并修复的预存 bug）

| 位置 | 问题 | 影响 | 修复 |
|------|------|------|------|
| `useSync.ts` `saveLocalData` | 只持久化基础 7 表 | FocusDesk/SummaryDesk 数据刷新后**静默丢失** | 补全 `fd*` / `summary*` / `userSettings` 字段 |
| `App.tsx` `handleDataFetched` 的 `hydrateState` | 只 hydrate 基础表 | 云端拉取后 FocusDesk/SummaryDesk 数据**不落盘** | 补全 `fdGoals/fdTasks/fdHabits/fdHabitLogs/summaryDocs/summaryIdeas/summaryLogs` |

这两处会导致跨设备同步时数据丢失，是上线级问题，已一并修复。

---

## 三、运行方式

```bash
# 本地开发（本地模式）
cd app && npm run dev

# 生产构建（已验证通过）
cd app && npm run build   # → dist/，619KB / gzip 166KB
```

**云端同步（需用户执行，属真实环境验证项）**：
1. 在 Supabase 项目的 SQL Editor 执行 `app/supabase-migration-unified.sql`（新增 8 表 + RLS，其中 `summary_ideas` / `summary_logs` 为本轮新增）
2. 配置 `app/.env` 的 `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`
3. Vercel 部署 → 注册唯一账号 → 换设备登录联调上传/下载

---

## 四、已知限制

- 换设备联调需真实 Supabase 环境执行迁移 SQL 后验证，当前仅有代码链路闭环 + 本地构建验证。
- bundle 存在「chunk > 500KB」的警告（非错误），后续可考虑按 Tab 代码分割优化首屏。

---

## 五、验证记录

```
> tsc -b && vite build
✓ 1818 modules transformed.
dist/assets/index-SVoIpzin.js   619.84 kB │ gzip: 166.24 kB
✓ built in 5.84s
```
