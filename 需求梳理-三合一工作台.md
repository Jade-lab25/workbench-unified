# 三合一工作台 · 需求梳理（v1 草稿）

> 整理日期：2026-09-09
> 状态：**待你确认**（确认后进入部署执行阶段）
> 一句话目标：以 Achieve-system 为壳，把 **FocusDesk（目标追踪）** 与 **Summary Desk（内容总结）** 合并进来，统一一个 Supabase 数据库、一套登录、一个入口，Vercel 公网部署。

---

## 0. 结论摘要

| 项 | 结论 |
|---|---|
| 三个网页是什么 | ① gswdb.cn = 工作状态记录（Achieve-system 成就系统，已公网运行）② workbuddy.link/Dkq… = Summary Desk 内容总结工作台 ③ workbuddy.link/4wg… = FocusDesk 目标待办工作台 |
| 合并可行性 | ✅ 代码已完成（`Workbench-Unified/app`，P1–P5 五阶段全部构建通过） |
| 当前卡点 | 不在代码，在**真实环境部署**：Supabase 迁移未执行、Vercel 未部署 |
| 数据方案 | 本地优先（localStorage）+ 手动双向云同步；16 张表全部 RLS（仅本人可读写） |
| 建议路线 | 确认本需求 → 我执行 Supabase 迁移 → Vercel 部署 → 锁死注册 → 双端联调 |

---

## 1. 三个源应用盘点（合并前）

### 1.1 Achieve-system（gswdb.cn · 工作状态记录）
- **定位**：本地优先的个人生产力应用，积分激励体系驱动习惯。
- **六大模块**：待办（完成 +5 分 / 拖延 −2 分）、打卡（任务型加分 / 消费型扣分）、时间记录（手动起止计时）、成就（积分流水日志）、灵感板（一键转待办）、成就商店（积分兑换）。
- **附加能力**：日历视图（日/月/年统计）、日期详情、数据同步面板（上传/下载/导入/导出）。
- **技术栈**：React 19 + TypeScript + Vite + Tailwind + Supabase（Auth + Postgres + RLS）。
- **部署现状**：gswdb.cn 线上运行中，**为 v1 版本**——存在已知硬伤：登录被硬编码绕过（形同虚设），任何人都能直接进入并看到数据；v2 已修复但未上线。

### 1.2 FocusDesk（目标待办工作台 · workbuddy.link/4wg…）
- **定位**：目标驱动的任务管理系统。
- **八大视图**：收集箱 / 今日 / 目标追踪 / 看板视图 / 日历视图 / 数据仪表盘 / 归档区 / 长期打卡。
- **任务模型**：P0/P1 优先级、逾期天数标记、目标进度百分比、目标拆解为子任务（拆解状态实时联动）、看板拖拽流转。
- **部署现状**：CodeBuddy 静态托管页（workbuddy.link 重定向到 workbuddy-space-static.codebuddy.work），无后端、数据仅存浏览器本地。

### 1.3 Summary Desk（内容总结工作台 · workbuddy.link/Dkq…）
- **定位**：灵感 + 工作日志 + AI 总结生成（日报/周报）。
- **四大视图**：灵感库（工作/个人成长两个方向，标签筛选）、工作日志（按日期）、总结生成（8 套模板：A 标准职场 / B 简明 / C 销售业务 / D 产品研发 / E OKR 对齐 / F 体制内 / G 标准日报 / H 简洁日报）、日历。
- **核心动作**：灵感条目可「转日志」「转素材」「编辑」「删除」；总结生成聚合范围内日志 + 灵感 + 素材，按方向分别生成。
- **LLM 模式**：浏览器直连大模型 API（DeepSeek / 火山方舟 / OpenAI / 智谱 / 自定义），60 秒超时。
- **部署现状**：CodeBuddy 静态托管页，无后端、无持久化（刷新即丢是原版痛点）。

---

## 2. 合并目标与边界

**目标**
1. 三个应用合并为一个统一工作台，**一个账号、一个数据库、一个入口**。
2. 保留三个应用各自的全部核心功能，不做功能删减（v1 已补齐全部视图）。
3. 公网部署：Supabase（后端）+ Vercel（前端静态托管），手机/PC 双端可用。

**边界（既定取舍，如需变更请指出）**
- 三个应用的历史数据**不迁移**，合并后全新开始。
- 沿用「本地优先 + 手动云同步」模式（数据先落 localStorage，点同步才上传，减少云端依赖）。
- **单人使用**：部署后注册唯一账号，并在 Supabase Auth 关闭公开注册，彻底锁死入口。
- 视觉沿用 Achieve-system 灰白简约风，不引入新设计系统。
- LLM 保持浏览器直连（不在后端代理，免服务器开销与密钥托管复杂度）。

---

## 3. 合并后功能清单（需求基线）

统一侧边栏导航，三大分组：

| 分组 | 功能页 | 来源 |
|---|---|---|
| **目标追踪** | 收集箱 / 今日 / 目标 / 看板 / 日历 / 仪表盘 / 归档区 / 习惯打卡 | FocusDesk |
| **工作状态** | 待办 / 打卡 / 时间记录 / 成就 / 成就商店 / 灵感 / 日历 | Achieve-system |
| **内容总结** | 总结生成 / 灵感库 / 工作日志 / 日历 | Summary Desk |

底部常驻：数据同步、登录状态。

**各功能页验收要点（v2 已实现）**

| 功能 | 验收要点 |
|---|---|
| 今日 | 当日任务列表、P0/P1 优先级、逾期标记、完成/开始操作 |
| 目标 | 目标进度、拆解子任务（叶子任务计入统计）、状态流转（active/paused/done） |
| 看板 | 任务按状态分列、拖拽流转 |
| 日历（目标） | 月历网格、按日事件、完成/未完成区分 |
| 仪表盘 | 范围筛选（7/30/90/全部/自定义）、KPI（新建/完成/进行中/逾期）、完成率环、优先级分布、趋势折线 |
| 归档区 | owner 筛选、恢复、永久删除 |
| 习惯打卡 | 长期打卡项目 + 按日记录（habit_id+date 去重） |
| 灵感库 | 方向/标签筛选、转日志、转素材、编辑、删除 |
| 工作日志 | 按日期浏览、增删改 |
| 总结生成 | 8 模板、聚合范围素材、LLM 生成、历史记录存云端 |
| 待办/打卡/时间/成就/商店 | 维持原版全部能力与积分规则 |
| 数据同步 | 手动上传/下载、JSON 导入导出、脏标记增量同步 |

---

## 4. 数据模型（Supabase，共 16 张表）

**基础 8 表**（原 Achieve-system，已有）
`todos` / `check_in_projects` / `check_in_records` / `time_records` / `achievement_logs` / `shop_items` / `inspirations` / `user_stats`

**新增 8 表**（`supabase-migration-unified.sql`，待执行迁移）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `fd_goals` | 目标 | title / status(active·paused·done) / progress(0-100) |
| `fd_tasks` | 任务+灵感统一模型 | fd_type(task·idea) / status(todo·doing·done·archived) / priority / due_date / subtasks(JSONB) |
| `fd_habits` | 长期打卡项目 | name / weekdays(JSONB) |
| `fd_habit_logs` | 打卡记录 | habit_id + log_date 唯一去重 |
| `summary_docs` | 总结历史 | kind(daily·weekly·custom) / template_code / summary / provider / model |
| `summary_ideas` | 灵感库 | content / tags(JSONB) / direction(work·growth·none) |
| `summary_logs` | 工作日志 | date / matter / result / data / issue |
| `user_settings` | 用户设置 | llm_provider / llm_api_key / llm_model（LLM Key 云端同步，换设备免重输） |

- 全部表启用 RLS，策略均为 `auth.uid() = user_id`（本人可读可写）。
- 主键为客户端生成 TEXT（延续原版离线可用设计），`synced_at` 记录同步时间。

---

## 5. 同步与一致性设计

沿用 Achieve-system 的核心同步模型（已历经 bug 修复，见 `docs/`）：

1. **本地优先**：所有数据写入 localStorage（`work-status-app-data`），刷新不丢。
2. **手动同步**：点「同步到云端」→ 脏标记增量上传 → 拉取云端最新 → 合并回本地。
3. **冲突策略**：本地脏 → 保留本地；两边干净 → 取 synced_at 较新；仅云端 → 收下；仅本地且云端已删 → 丢弃。
4. **删除**：墓碑机制（已删 ID 列表），同步时批量删云端。
5. **两类记录**：checkInRecords / achievementLogs 为 append 型（只追加）；其余为 upsert 型（可修改）。
6. **v2 已修复两个上线级 bug**：`saveLocalData` 漏持久化新模块数据（刷新静默丢失）、`hydrateState` 漏回填云端拉取数据（跨设备不同步）。

---

## 6. 登录与安全

| 项 | 方案 |
|---|---|
| 登录方式 | Supabase Auth 邮箱登录（密码） |
| 双模式 | 本地模式（跳过登录，仅用 localStorage，云同步按钮置灰）/ 云端模式（登录后自动拉取云端数据） |
| 入口锁死 | 部署后注册唯一账号 → Supabase 控制台关闭「允许新用户注册」（Authentication → Providers → Email → 禁用 sign up） |
| 数据隔离 | RLS 全表覆盖：未登录/他人访问拿不到任何数据 |
| 密钥管理 | Supabase URL/anon key 走环境变量（VITE_ 前缀构建注入）；LLM Key 存 user_settings 表（RLS 保护），不入代码库 |
| v1 遗留 | 现网 gswdb.cn 存在登录硬编码绕过问题（任何人可进），v2 已修复，部署后即消除 |

---

## 7. 部署架构（Supabase + Vercel）

```
用户浏览器（手机 / PC）
   │  HTTPS
   ▼
Vercel 静态托管（React 19 SPA，dist/）
   │  Supabase JS SDK（anon key + RLS 兜底）
   ▼
Supabase
   ├─ Auth：邮箱登录（唯一账号，关闭公开注册）
   ├─ Postgres：16 张表，全表 RLS
   └─（可选）LLM Key 存 user_settings
   │
   ▼（浏览器直连，不经服务器）
LLM 供应商 API：DeepSeek / 火山方舟 / OpenAI / 智谱 / 自定义
```

**部署四步**（均需真实环境操作）：
1. **Supabase 迁移**：SQL Editor 执行 `app/supabase-migration-unified.sql`（新增 8 表 + 索引 + RLS）。
2. **Vercel 部署**：`app/` 目录构建（`npm run build`，已验证通过，产物 619KB / gzip 166KB），配置环境变量 `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`。
3. **注册锁号**：部署后注册唯一账号 → 关闭公开注册。
4. **双端联调**：手机 + PC 各登录一次，验证上传/下载、总结生成、习惯打卡同步。

---

## 8. 现状盘点（差距分析）

**已完成 ✅**
- 三合一代码开发完成（P1 硬伤修复 / P2 导航重构 / P3 FocusDesk 补齐 / P4 SummaryDesk 补齐 / P5 同步扩展），`npm run build` 通过
- 两个跨设备丢数据 bug 修复
- `app/.env` 已配置 Supabase URL + anon key
- 迁移 SQL 已写好（`supabase-migration-unified.sql`）
- 本地构建产物 `dist/` 就绪

**未完成 ⏳（需真实环境 / 需你操作）**
- Supabase 项目执行迁移 SQL（新增 8 表 + RLS）
- Vercel 项目创建与部署（当前无 vercel.json、无 .vercel 链接、无 git 仓库）
- 唯一账号注册 + 关闭公开注册
- 双设备云同步联调
- LLM Key 配置（登录后在「内容总结」页填写，存 user_settings）

**工程建议 🟡**
- 当前 `Workbench-Unified` **不是 git 仓库**，代码无版本管理。建议 `git init` + 首次提交（中文提交说明），防止误删/回退困难。
- 无 `vercel.json`：Vite SPA 直出一般可用，但建议补一个 SPA rewrite（`rewrites → index.html`）兜底深链刷新 404。
- bundle 619KB 存在「chunk > 500KB」警告，后续可按 Tab 做代码分割优化首屏（非阻塞）。

---

## 9. 待你确认的决策点

1. **部署域名**：合并版替换 gswdb.cn（把域名绑到 Vercel），还是 Vercel 默认新域名（`xxx.vercel.app`）？
2. **历史数据**：三个应用旧数据确定不迁移、全新开始？（沿用既定约定，默认「是」）
3. **LLM 默认供应商**：总结生成默认推荐哪家？（代码支持 DeepSeek / 火山方舟 / OpenAI / 智谱 / 自定义，可在应用内切换）
4. **git 初始化**：是否现在就 `git init` 并把 Workbench-Unified 纳入版本管理？（建议做）
5. **部署方式**：Vercel 需要你提供授权（Vercel CLI token / 登录 Vercel 网页 / GitHub 关联），你倾向哪种？

---

## 10. 下一步（确认后执行）

1. 你确认本需求基线 + 回答第 9 节决策点（尤其部署方式和域名）。
2. 我执行：git 初始化 → Supabase 迁移脚本落地核对 → Vercel 部署配置（含 vercel.json）→ 构建部署。
3. 你注册账号 → 我协助关闭公开注册 → 双端联调验证（数据上传/下载、总结生成、同步）。
