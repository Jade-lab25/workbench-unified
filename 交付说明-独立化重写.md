# 独立化重写 · 交付说明（三合一工作台 v2）

> 状态：✅ 已完成代码重写 + 构建验证 + 浏览器冒烟测试
> 日期：2026-09-09

## 更新记录

### v2.5（日历对齐 FocusDesk）

- **全部格子等高**：月视图格子固定高度（桌面 96px / 移动 88px），内容溢出自动截断，不再被任务撑高——实测 30 个格子高度完全一致
- **点击格子里的事项直接编辑**：点任务 → 弹出完整「任务详情」编辑器（复用目标页编辑器：标题/优先级/截止日期/所属目标/标签/状态/备注/子任务拆解/归档/删除）；点完成待办 → 轻量标题编辑器。改完格子和详情面板实时刷新
- **统一月视图**：移除周/月切换，移动端默认月视图，对齐 FocusDesk 形态
- **顶部今日统计条**：今日已完成 N 项 · 还有 N 项未完成（对齐 FocusDesk）

### v2.4（打卡对齐 Achieve-system）

- **一天内可多次打卡**：去掉「今日已打」限制，按钮常亮可重复点击，每次记录一条打卡 + 积分
- **累计打卡次数**：实时统计该项目全部历史打卡次数，显示「累计 N 次」
- **今日次数**：当天打卡次数单独显示（多次打卡时能直观看到今天打了几次）
- **最后打卡时间**：显示「最后打卡 9月9日 17:46」，格式对齐原版截图
- 按钮配色按项目类型：任务型绿色（+N）、消费型红色（-N），对齐原版

### v2.3（日历视图重做）

- **每个日期格子显示「当天成就值」**：参考图三，有成就增量的日子显示 `+52` 绿色徽标（负分红色）
- **每个日期格子显示「当天待办事项」**：参考图二，格子内直接列任务文字（任务◆带 P0红/P1黄优先级色、完成待办✓绿色），超过 2-3 条显示「+N 更多」，其余类型（打卡/习惯/日志/灵感/时间）保留小圆点
- **移动端（≤700px）默认周视图**：一周 7 天大格子，任务文字清晰可读，周切换 + 今天按钮；可切回紧凑月视图
- **本月汇总条**：本月成就 / 完成待办 / 打卡 / 专注时长（参考图三）
- 当日详情面板升级：成就值 + 各类事件分组展示
- 桌面端月视图格子加高（min-height 96px），容纳成就值与任务文字

### v2.2（抽屉修复）

- **修复移动端菜单栏不弹出**：根因是响应式规则 `.sidebar { display: none }` 把抽屉容器内的侧边栏也一并隐藏了——点击后抽屉其实已打开，但内容是透明的空容器，看起来"什么都没发生"。已改为仅隐藏桌面侧边栏（`.app > .sidebar`），并强制抽屉内侧边栏显示（`.drawer .sidebar { display: flex }`）

### v2.1（计时器 + 任务编辑）

- **计时器秒级跳动**：工作 → 时间，进行中的计时以大号 HH:MM:SS 每秒刷新（合计、当日记录行同步跳动）；待办计时徽标同样每秒跳动
- **恢复任务编辑（FocusDesk 能力）**：点击看板卡片 / 目标下任务行 / 铅笔按钮打开任务编辑器
  - 可改：内容 / 优先级 / 截止日期 / 所属目标 / 标签 / 状态 / 备注
  - 子任务拆解：添加 / 勾选 / 删除，且支持再拆一层（嵌套子步骤）
  - 一键归档 / 永久删除（带确认）
- **目标可编辑**：点目标标题或铅笔按钮 → 改名 / 改描述 / 切换状态（进行中·已暂停·已完成）
- 新增 store action：`addFdSubtaskChild`（嵌套子任务），已导出并同步

---

## 一、结论

三个页面应用（gswdb.cn 工作状态 / FocusDesk 目标待办 / SummaryDesk 内容总结）已合并重写为**单页面工作台**：
不再是三个 app 拼凑，而是统一设计系统、统一信息架构、统一数据层的一个应用。**代码为全新编写**（未沿用 workbuddy 平台环境代码），可正常用于 Vercel + Supabase 公网部署。

## 二、信息架构（7 项一级导航，替代原双导航 9+8）

| 导航 | 内容 |
|---|---|
| 今日 | 5 个 KPI（待办/打卡/专注/今日成就/累计）+ 今日待办 + 目标任务 + 习惯打卡 + 时间 + 快捷记录 |
| 目标 | 目标列表（含拆解进度）/ 看板（待处理·进行中·已完成）/ 习惯打卡（近 12 周热力格）/ 归档 |
| 工作 | 待办 / 打卡 / 时间 / 成就 / 商店 / 灵感 六个子页 |
| 总结 | AI 生成（日报·周报·自定义，8 套模板，4 家 LLM 供应商）/ 灵感库 / 工作日志 |
| 日历 | 统一日历：待办·打卡·任务·习惯·日志·灵感 六色圆点 + 当日详情 |
| 统计 | 完成率环 + 优先级分布 + 完成趋势 + 目标进度 + 习惯/成就/专注汇总 |
| 设置 | 账号 / 云同步 / LLM API / 数据备份 |

## 三、保留的核心资产（复用不改）

- **16 张表数据模型**（`app/supabase-migration-unified.sql`，全 RLS `auth.uid()=user_id`）
- **同步协议**（脏标记 / 墓碑 / upsert / append 去重，`hooks/useSync.ts` + `supabase/database.ts` 原样保留）
- **积分规则**（完成 +5 / 拖延 -2 / 打卡 ±points / 商店扣分，`store/index.ts` 原样保留）
- **store actions** 全部保留（视图按原签名调用）

## 四、全新编写的部分

```
app/src/index.css                    设计系统（单一强调色 #3B6EF6、8px 栅格、浅色极简、≤900px 抽屉）
app/src/utils/dates.ts / llm.ts / stats.ts   日期 / LLM 供应商+8 模板 / 统一统计口径（纯函数）
app/src/components/ui.tsx            Seg / Modal / Empty / Progress / Switch / Toast / 安全 Markdown
app/src/components/Sidebar.tsx       7 项导航 + 移动端抽屉复用
app/src/components/AuthScreen.tsx    登录/注册/本地模式
app/src/App.tsx                      壳：登录门、云端/本地模式、同步接线、侧边栏角标
app/src/views/{Today,Goals,Work,Summary,Calendar,Stats,Settings}View.tsx   7 个视图
旧组件已移入 app/_legacy-components/（仅作参照，不参与编译）
```

## 五、验证结果

| 项 | 结果 |
|---|---|
| `npm run build`（tsc -b + vite build） | ✅ 通过，无 TS 错误 |
| 产物 | 4 个 chunk，gzip 合计约 158 kB（React/Supabase/图标分包，无 500kB 大块警告） |
| 浏览器冒烟（localhost:4173 生产构建） | ✅ 登录页渲染 → 本地模式进入 → 今日视图 KPI → 抽屉导航 → 7 个视图全部挂载 → 添加待办端到端通过（列表渲染+角标+Toast） |
| 控制台 | 无 JS 错误 |

## 六、部署（Supabase + Vercel）

1. **Supabase**：建项目 → SQL Editor 执行 `app/supabase-migration-unified.sql` → 认证开启 Email 注册（部署后建议关注册）
2. **环境变量**：`app/.env` 已有 `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY`（勿提交，.env.example 留模板）
3. **Vercel**：项目根选 `app/` 目录，Framework=Vite，构建 `npm run build`，输出 `dist`
   - `vercel.json` 已内置 SPA rewrite（刷新不 404）
   - 授权方式待定：CLI token / 网页导入 / GitHub 关联
4. 公网访问即部署 URL；原 gswdb.cn 是否替换待你拍板

## 七、待拍板项（不影响代码，部署前确认）

1. 部署域名：Vercel 默认域名 or 替换 gswdb.cn
2. 历史数据：此前约定全新开始，不迁移（gswdb 线上 v1 数据不动）
3. LLM 默认供应商：deepseek（已默认，代码全支持 volcengine/openai/zhipu/custom）
4. 是否 `git init` 纳入版本管理
5. Vercel 授权方式（CLI token / 网页 / GitHub）
